# Loaded by CI with `rpmlint -r packaging/rpm/eitaas-linux.rpmlintrc`.
# The GUI imports libadwaita through GObject introspection; no automatic
# dependency generator covers that, so the explicit Requires is intentional.
addFilter(r"explicit-lib-dependency libadwaita")
